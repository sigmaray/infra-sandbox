services:
  go-blog:
    build: .
    container_name: go-blog
    env_file: .env
    ports:
      - "${GO_BLOG_HTTP_PORT:-8083}:8083"
    restart: unless-stopped
    networks:
      - projects-net

networks:
  projects-net:
    external: true
    name: projects-net
